# VAST DB Connection Details
# Copy this file to .env and fill in your actual credentials
# Make sure .env is added to your .gitignore file!

VAST_DB_ENDPOINT="http://<your-vast-endpoint-here>"
VAST_ACCESS_KEY="<your-access-key-here>"
VAST_SECRET_KEY="<your-secret-key-here>"

# Optional: uncomment and set if you need to disable SSL verification (not recommended for production)
# VAST_VERIFY_SSL=False

# --- MCP Server Configuration ---
# Comma-separated list of allowed SQL statement types (e.g., SELECT, INSERT, UPDATE, DDL for CREATE/ALTER/DROP)
# Defaults to "SELECT" if not set.
MCP_ALLOWED_SQL_TYPES="SELECT" 